// Note: This function is not used in React Native
// Image cropping is handled by expo-image-picker's built-in cropping
// This is a stub to prevent import errors
export default function getCroppedImg(imageSrc, croppedAreaPixels, rotation = 0) {
  // This function is not used in React Native
  // expo-image-picker handles cropping natively
  return Promise.resolve(null);
}
