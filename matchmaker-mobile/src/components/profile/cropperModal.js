// Note: This component is not used in React Native
// Image cropping is handled by expo-image-picker's built-in cropping
// This is a stub to prevent import errors
import React from 'react';

const CropperModal = ({ imageSrc, onClose, onCropComplete }) => {
  // This component is not used in React Native
  // expo-image-picker handles cropping natively
  return null;
};

export default CropperModal;
